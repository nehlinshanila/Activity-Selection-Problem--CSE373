import random
Random_start = 1
Random_end = 10
# Will generate a random integer between Random_start and Random_end
random_number = random.randint(Random_start, Random_end)
print(random_number)


# i will update thhe Random_start and Random_end 
# with every iteration. This will generate a random number between
# the start and end and keep pushing them into the array for the sorting
# algorithm to sort over time.